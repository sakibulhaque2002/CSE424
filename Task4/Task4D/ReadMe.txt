For "EfficientNet b7 with XAI" the model was trained for only 2 epochs because it reaches the expected accuracies just with 2 iterations and it saves time. The model weights could not be loaded from the original 
notebook, "EfficientNet b7"

"Traffic Signs XAI" contains explanations of MobileNet V2 and MobileNet V3 models that were trained in "Traffic_Signs_MNV2" and "MNV3_TRAFFICSIGNS_FT" respectively.
